<svg width="300" height="300" viewBox="0 0 300 300" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <radialGradient id="glow" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#c11b63" stop-opacity="1"/>
      <stop offset="100%" stop-color="#c11b63" stop-opacity="0"/>
    </radialGradient>
    <filter id="neon" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="0" stdDeviation="4" flood-color="#c11b63"/>
    </filter>
  </defs>

  <!-- Outer ring -->
  <circle cx="150" cy="150" r="100" stroke="#c11b63" stroke-width="4" fill="none" filter="url(#neon)"/>

  <!-- Inner ring -->
  <circle cx="150" cy="150" r="90" stroke="#c11b63" stroke-width="2" fill="none" filter="url(#neon)"/>

  <!-- Text -->
  <text x="150" y="145" text-anchor="middle" font-size="24" fill="white" font-family="sans-serif">Let’s</text>
  <text x="150" y="175" text-anchor="middle" font-size="24" fill="white" font-family="sans-serif">Connect</text>
</svg>